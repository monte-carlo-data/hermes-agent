agent-common @ git+https://github.com/monte-carlo-data/agent-common.git@mrostan/in-process-log-shipping
# When bumping this version, also bump the corresponding `<version>-system-base`
# image tag in the Dockerfile FROM so the runtime image matches the python pin.
mcd-agent @ git+https://github.com/monte-carlo-data/apollo-agent.git@v1.6.0
