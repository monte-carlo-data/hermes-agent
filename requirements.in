# TEMP (dev validation): pinned to the agent-common feature branch; flip to the released tag after agent-common PR #25 merges.
agent-common @ git+https://github.com/monte-carlo-data/agent-common.git@mrostan/yet-2364-report-credential-id-in-reachability-result
# When bumping this version, also bump the corresponding `<version>-system-base`
# image tag in the Dockerfile FROM so the runtime image matches the python pin.
mcd-agent @ git+https://github.com/monte-carlo-data/apollo-agent.git@v1.11.1
