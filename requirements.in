agent-common @ git+https://github.com/monte-carlo-data/agent-common.git@v0.2.5
# When bumping this version, also bump the corresponding `<version>-system-base`
# image tag in the Dockerfile FROM so the runtime image matches the python pin.
# Note: using ``@dev`` here keeps the same base image (no Python pin change) and
# only refreshes the apollo-agent code — temporary while we test the external
# credentials cache (apollo-agent PR #300) in dev. Replace with a tagged pin
# once the upstream PR lands in main.
mcd-agent @ git+https://github.com/monte-carlo-data/apollo-agent.git@dev
